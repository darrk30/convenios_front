import { ArchivosFormModalComponent } from './archivos-form-modal.component'

describe('ArchivosFormModalComponent', () => {
  it('should mount', () => {
    cy.mount(ArchivosFormModalComponent)
  })
})