import { ResultadosFormModalComponent } from './resultados-form-modal.component'

describe('ResultadosFormModalComponent', () => {
  it('should mount', () => {
    cy.mount(ResultadosFormModalComponent)
  })
})