import { TiposConveniosListComponent } from './tipos-convenios-list.component'

describe('TiposConveniosListComponent', () => {
  it('should mount', () => {
    cy.mount(TiposConveniosListComponent)
  })
})