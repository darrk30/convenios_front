import { PerfilesFormModalComponent } from './perfiles-form-modal.component'

describe('PerfilesFormModalComponent', () => {
  it('should mount', () => {
    cy.mount(PerfilesFormModalComponent)
  })
})