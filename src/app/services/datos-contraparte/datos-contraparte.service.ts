import { Injectable } from '@angular/core';
import { Observable, of } from 'rxjs';
import { Integrante } from '../../models/integrante.model';
import { Coordinador } from '../../models/coordinador.model';

@Injectable({
  providedIn: 'root'
})
export class DatosContraparteService {

  
}